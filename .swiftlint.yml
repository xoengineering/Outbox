included:
  - App
  - OutboxKit/Sources
  - OutboxKit/Tests
line_length: 125
identifier_name:
  min_length: 2
nesting:
  type_level: 2
type_body_length:
  warning: 350
disabled_rules:
  - opening_brace # swift-format owns formatting; it wraps braces after multiline conditions
  - todo
  - trailing_comma # swift-format owns formatting and inserts trailing commas
