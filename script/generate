#!/bin/sh
# script/generate: regenerate Outbox.xcodeproj from project.yml.

set -e

cd "$(dirname "$0")/.."

if [ ! -f Local.xcconfig ]; then
  cp Local.xcconfig.example Local.xcconfig
  echo "Created Local.xcconfig — set DEVELOPMENT_TEAM in it to sign locally."
fi

xcodegen generate
