#!/bin/sh
# script/lint: check formatting (Apple structure) and style (community conventions).
# --strict on both so findings fail the build instead of scrolling past.

set -e

cd "$(dirname "$0")/.."

swift format lint --strict --recursive App OutboxKit
swiftlint lint --strict --quiet
